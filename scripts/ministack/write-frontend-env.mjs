/**
 * Writes frontend/.env.local from the FrontEndEnvironment output of the local
 * stack, so `next dev` talks to the emulator rather than the deployed dev
 * environment. The same output is used for real deployments, which the README
 * reads with `aws cloudformation describe-stacks`.
 *
 * frontend/.env.local is overwritten, so keep any deployed dev configuration
 * you need somewhere else.
 *
 * Usage:
 *   docker compose up -d
 *   pnpm ministack:env
 */
import { execFileSync } from "node:child_process";
import { writeFileSync } from "node:fs";
import { resolve } from "node:path";

const endpoint = process.env.MINISTACK_ENDPOINT ?? "http://localhost:24566";
const region = process.env.AWS_REGION ?? "ap-southeast-2";
const stackName = process.env.API_STACK_NAME ?? "local-transcription";
const timeoutSeconds = Number(process.env.MINISTACK_DEPLOY_TIMEOUT ?? 600);

const GENERATED_HEADER =
  "# Generated by pnpm ministack:env from the local stack outputs. Edits are lost on the next run.";

const envPath = resolve(import.meta.dirname, "../../frontend/.env.local");

/**
 * Driven through the AWS CLI to match the other MiniStack scripts, none of
 * which pull an SDK client into a workspace just to read the emulator.
 */
const aws = (args) => {
  try {
    return execFileSync(
      "aws",
      ["--endpoint-url", endpoint, "--region", region, ...args],
      {
        encoding: "utf8",
        stdio: ["ignore", "pipe", "pipe"],
        env: {
          ...process.env,
          AWS_ACCESS_KEY_ID: "test",
          AWS_SECRET_ACCESS_KEY: "test",
        },
      },
    );
  } catch (error) {
    const stderr = error.stderr?.toString() ?? "";
    if (stderr.includes("does not exist")) return undefined;
    throw new Error(`aws ${args.join(" ")} failed: ${stderr || error.message}`);
  }
};

const readFrontEndEnvironment = () =>
  aws([
    "cloudformation",
    "describe-stacks",
    "--stack-name",
    stackName,
    "--query",
    "Stacks[0].Outputs[?OutputKey=='FrontEndEnvironment'].OutputValue",
    "--output",
    "text",
  ])?.trim();

const sleep = (ms) => new Promise((done) => setTimeout(done, ms));

/**
 * `docker compose up -d` returns once MiniStack is healthy, but the bootstrap
 * container is still running cdklocal at that point, so the stack does not
 * exist yet on a cold start. Poll rather than requiring a separate wait step.
 */
const waitForFrontEndEnvironment = async () => {
  const deadline = Date.now() + timeoutSeconds * 1000;
  let announced = false;
  while (true) {
    const value = readFrontEndEnvironment();
    if (value) return value;
    if (Date.now() > deadline) {
      throw new Error(
        `No FrontEndEnvironment output on ${stackName} after ${timeoutSeconds}s. Check pnpm ministack:logs.`,
      );
    }
    if (!announced) {
      console.log(`waiting for ${stackName} to finish deploying...`);
      announced = true;
    }
    await sleep(5000);
  }
};

const frontEndEnvironment = await waitForFrontEndEnvironment();

writeFileSync(envPath, `${GENERATED_HEADER}\n${frontEndEnvironment}\n`);

console.log(`wrote ${envPath} from ${stackName}`);
